import contact from '../assets/contact.json';
import profile from '../assets/profile.json';

export const landingRaw = {
  profile,
  contact: {
    email: contact.email,
    telegram: contact.telegram,
  },
};
